const testCase = require('./test-case').testCase
// a function that returns the max number of unique types after half is removed

function solution(T) {
  return 0;
}




const list = [
    [solution([0]), 1], 
    [solution([1, 2]), 2], 
    [solution([0, 0, 1]), 2]];

testCase(list);
